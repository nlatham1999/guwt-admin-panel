import React, { Fragment, useState} from "react";
import { render } from "react-dom";

const MediaView = () => {

    return (
        <div>
            <h1>media view</h1>
        </div>
    );
}

export default MediaView;